# Metabase-Swarm-Deployment
Deploying Metabase on Docker Swarm behind an existing nginx reverse proxy, with a dedicated Postgres metadata DB and a connection guide for Citus/Patroni clusters. Stage → production, no exposed ports.
